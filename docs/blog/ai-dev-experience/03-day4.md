# AI 활용 개발 경험 정리 — Day 4

## 오늘 AI에게 맡긴 작업

1. CORS 글로벌 설정 코드 생성
2. GlobalExceptionHandler 작성
3. 프론트엔드 에러 처리 로직 개선
4. 로딩 UI 스피너 구현
5. 카드 디자인 개선 + 애니메이션

---

## 1. CORS 글로벌 설정

### 프롬프트 (요약)

```
@CrossOrigin을 글로벌 WebMvcConfigurer로 전환해줘. origin은 properties에서 읽도록.
```

### AI 결과

`WebConfig.java` 생성 + `@Value`로 properties 연동. 수정 없이 바로 동작.

---

## 2. GlobalExceptionHandler

### 프롬프트 (요약)

```
글로벌 예외 핸들러 추가해줘. Claude API 실패, 파싱 실패 등에 대한 적절한 HTTP 응답.
```

### AI가 생성한 코드

```java
@RestControllerAdvice
public class GlobalExceptionHandler {

    @ExceptionHandler(ResponseStatusException.class)
    public ResponseEntity<Map<String, String>> handleStatus(ResponseStatusException e) {
        return ResponseEntity.status(e.getStatusCode())
            .body(Map.of("error", e.getReason() != null ? e.getReason() : "Bad Request"));
    }

    @ExceptionHandler(RuntimeException.class)
    public ResponseEntity<Map<String, String>> handleRuntime(RuntimeException e) {
        return ResponseEntity.status(HttpStatus.BAD_GATEWAY)
            .body(Map.of("error", "AI 서비스 연동 중 오류가 발생했습니다."));
    }

    @ExceptionHandler(Exception.class)
    public ResponseEntity<Map<String, String>> handleGeneral(Exception e) {
        return ResponseEntity.status(HttpStatus.INTERNAL_SERVER_ERROR)
            .body(Map.of("error", "서버 내부 오류가 발생했습니다."));
    }
}
```

예외 계층 구조를 잘 분리해서 생성해줬다. 수정 없이 사용.

---

## 3. 프론트엔드 에러 처리

### 프롬프트 (요약)

```
프론트에서 백엔드 에러 메시지를 파싱하고, 네트워크 에러와 서버 에러를 구분해줘.
```

### AI가 생성한 코드

```typescript
if (!res.ok) {
  const body = await res.json().catch(() => null);
  throw new Error(body?.error || `서버 오류 (${res.status})`);
}
// ...
catch (err) {
  if (err instanceof TypeError) {
    setError("서버에 연결할 수 없습니다. 백엔드가 실행 중인지 확인해주세요.");
  } else {
    setError(err instanceof Error ? err.message : "요약 중 오류가 발생했습니다.");
  }
}
```

`TypeError`로 네트워크 에러를 잡는 패턴은 fetch API의 특성을 잘 활용한 것.

---

## 4. 로딩 스피너 + 카드 개선

### 프롬프트 (요약)

```
로딩 중 textarea 비활성화 + 버튼에 스피너 추가. 카드에 색상 구분 + fade-in 애니메이션.
```

### AI 결과

- SVG 스피너 + `animate-spin` (TailwindCSS 유틸리티)
- 카드 `border-l-4` + 색상 구분
- CSS `@keyframes fadeIn` + arbitrary animation class

프론트엔드 UI 코드는 역시 AI가 빠르고 정확하게 생성한다.

---

## Day 4 AI 활용 총평

| 작업 | AI 기여도 | 비고 |
|------|----------|------|
| CORS 글로벌 설정 | 100% | 수정 없이 동작 |
| GlobalExceptionHandler | 100% | 예외 계층 분리 정확 |
| 프론트 에러 처리 | 95% | TypeError 패턴 제안 우수 |
| 로딩 스피너 | 100% | SVG + TailwindCSS 조합 |
| 카드 디자인 | 100% | 색상 + 애니메이션 한 번에 |

### 오늘 배운 것

- **Day 4는 삽질이 없었다.** Day 3에서 기본 연동이 끝나니, 보강 작업은 AI에게 맡기면 거의 수정 없이 완성된다.
- **에러 처리는 백엔드-프론트 양쪽을 동시에 설계해야 한다.** 백엔드가 내려주는 에러 형식을 먼저 정하고, 프론트가 그걸 파싱하는 순서가 맞다.
- **"보강" 작업은 AI 효율이 가장 높은 영역.** 기존 코드에 기능을 추가하는 건 컨텍스트가 명확해서 AI가 정확하게 처리한다.
